import { ToggleFeatureDirective } from './toggle-feature.directive';

describe('ToggleFeatureDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleFeatureDirective(null);
    expect(directive).toBeTruthy();
  });
});
